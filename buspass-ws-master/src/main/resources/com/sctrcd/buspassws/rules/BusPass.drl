package com.sctrcd.busspassws.rules

import com.sctrcd.buspassws.model.*;


rule "minusTenPercent"
    no-loop
    when
       $i: ItemCountUser( getItems().size() < 2 )
    then
        System.out.println("droolsika poz");
        modify($i){ getItems().clear(); }
    end

